#include <unistd.h>

/**
 * main - where the whole code resides
 * Description: we're printing to the standard error stream
 * Return: 1
 */
int main(void)
{
write("and that piece of art is useful\" - Dora Korpar, 2015-10-19\n");
return (1);
}
