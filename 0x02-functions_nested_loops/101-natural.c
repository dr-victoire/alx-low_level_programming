#include <stdio.h>

/**
 * main - prints the sum of multiples of 3 and 5
 *
 * Return: 0 (Successful)
 */

int main(void)
{
	int a;
	int sum;

	for (a = 0; a < 1024; a++)
	{
		if ((a % 3 == 0) || (a % 5 == 0))
		{
			sum += i;
		}
	}
	printf("%d\n", sum);

	return (0);
}
