#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main -the place where the code goes
 * Description: conditional for c
 * Return: 0 (Successful)
 */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	if (
	return (0);
}
