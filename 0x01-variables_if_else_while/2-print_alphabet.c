#include <stdio.h>

/**
 * main - the function where the code stays
 * Description: prints alphabets in lowercase
 * Return: 0 (Successful)
 */
int main(void)
{
	for(int x = 'a'; x <= 'z'; x++)
	{
		putchar(x);
	}
	return (0);
}
