#include <stdio.h>

/**
 * main - function that bears all codes
 * Description: Printing no from 0 - 9
 * Return: 0 (Successful)
 */
int main(void)
{
	int a = 0;

	while (a < 10)
	{
		printf("%d", a);
		a++;
	}
	printf("\n");
	return (0);
}
