#include "main.h"

/**
 * main - testing the function that shows whether or not a number
 * is positive or negative
 * Return: 0 (Successful)
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
